const modes={live:{stage:'Live',lead:'配信、アーカイブ、グッズ、SNS、案件相談がばらばらになりがちな活動中VTuber向けに、今の活動をもっと見やすく、もっと推しやすく、もっと仕事につながる形へ整えます。',date:'今日 21:00',title:'今週の定期配信',copy:'近況、今週のお知らせ、アーカイブ導線をまとめます。'},game:{stage:'Gaming',lead:'参加ルール、待機リンク、切り抜き導線、アーカイブをまとめて、初見さんでも入りやすいゲーム配信ページにできます。',date:'金曜 22:00',title:'みんなで遊ぶゲーム配信',copy:'参加方法、コメントルール、待機リンクを事前に確認できます。'},song:{stage:'Song',lead:'歌枠、セットリスト、歌ってみた投稿、リクエスト導線をかわいく整理し、次の音楽企画につながる見せ方にします。',date:'日曜 20:00',title:'おやすみ前の歌枠',copy:'セットリスト、リクエスト、アーカイブ導線をまとめます。'}};const $=s=>document.querySelector(s);const $$=s=>document.querySelectorAll(s);const body=document.body;const loader=$('[data-loader]');const progress=$('[data-progress]');const cursor=$('[data-cursor]');const nav=$('#nav');const toggle=$('.nav-toggle');const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;function setText(s,v){$$(s).forEach(n=>n.textContent=v)}function applyMode(name){const mode=modes[name]||modes.live;setText('[data-stage]',mode.stage);setText('[data-lead]',mode.lead);setText('[data-card-date]',mode.date);setText('[data-card-title]',mode.title);setText('[data-card-copy]',mode.copy);$$('[data-mode]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.mode===name)))}function updateProgress(){const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max<=0?0:Math.min(100,scrollY/max*100)}%`}function reveal(){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('is-visible')}),{threshold:.14});$$('[data-reveal]').forEach(n=>io.observe(n))}function tilt(){if(reduce)return;$$('[data-tilt]').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${y*-4}deg) rotateY(${x*5}deg) translateY(-3px)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}if(toggle)toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.classList.toggle('is-open',!open)});$$('[data-mode]').forEach(b=>b.addEventListener('click',()=>applyMode(b.dataset.mode)));$$('.nav a').forEach(a=>a.addEventListener('click',()=>{toggle?.setAttribute('aria-expanded','false');nav?.classList.remove('is-open')}));$('[data-submit]')?.addEventListener('click',()=>alert('デモ送信です。実案件ではフォーム、LINE、メール、スプレッドシート連携に差し替えます。'));if(!reduce){addEventListener('pointermove',e=>{cursor.classList.add('is-visible');cursor.style.transform=`translate(${e.clientX-95}px,${e.clientY-95}px)`});addEventListener('pointerdown',e=>{const s=document.createElement('span');s.className='sparkle';s.style.left=e.clientX+'px';s.style.top=e.clientY+'px';document.body.appendChild(s);setTimeout(()=>s.remove(),760)})}addEventListener('scroll',updateProgress,{passive:true});addEventListener('resize',updateProgress);addEventListener('load',()=>setTimeout(()=>loader?.classList.add('is-hidden'),380));reveal();tilt();updateProgress();applyMode(new URLSearchParams(location.search).get('mode')||'live');
